#include "main.h"
/**
 * _abs - compute absolute value of an integer
 * @n: the int to be computed
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
